import { DayHourMinutePipe } from './day-hour-minute.pipe';

describe('DayHourMinutePipe', () => {
  it('create an instance', () => {
    const pipe = new DayHourMinutePipe();
    expect(pipe).toBeTruthy();
  });
});
